mutation changePassword($newPassword: String!, $confirmPassword: String!, $newPasswordToken: String!, $id: Int!) {
    changePassword(newPassword: $newPassword, confirmPassword: $confirmPassword, newPasswordToken: $newPasswordToken, id: $id ) {
        message
    }
}
