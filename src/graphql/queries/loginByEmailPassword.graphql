mutation loginByEmailPassword($email: String!, $password: String!) {
    generateCustomerToken (email: $email, password: $password) {
        token
        firstname
        lastname
        customer_id
    }
}