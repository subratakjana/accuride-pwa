mutation subscribeStockNotice($product_id: Int!, $parent_id: Int!, $website_id: Int!, $email: String!){
  subscribeStockNotice(
    product_id: $product_id,
    parent_id: $parent_id,
    website_id: $website_id,
    email: $email
  ) {
    message
    params
  }
}