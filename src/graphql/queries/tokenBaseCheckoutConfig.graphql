query tokenBaseCheckoutConfig {
  tokenBaseCheckoutConfig(method: "paradoxlabs_cybersource") {
    hasVerification
    cvvImageUrl
    cardinalAuthUrl
    cardinalJWT
    cardinalScript
    fingerprintUrl
    paramUrl
  }
}