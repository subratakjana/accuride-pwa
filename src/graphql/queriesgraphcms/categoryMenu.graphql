query blogcategories{
  categories{
    id
    categoryTitle
    categorySlug
    parentCategory {
      id
      categoryTitle
      categorySlug
    }
    categories {
      id
      categoryTitle
    }
  }
}